# Week 15 — End-to-end ticket desk

Form/email → Dify (classify + retrieve + draft + confidence) → n8n (write, notify, approve, send).
Trace `ticket_id` across Dify, n8n, and Langfuse. Record a 3-minute demo.
