# Week 16 — Eval, safety, cost, deploy

Golden set in CI. Tool least-privilege. Small model for classify/extract, large model for hard calls.
One-command compose for API + Postgres. Write `ARCHITECTURE.md`.

**Pass bar:** Swapping a model tells you quality went up or down. Send tools never auto-run.
