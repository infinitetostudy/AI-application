# Week 03 — Context engineering

Treat the context window as a budget. Compare prompt versions with accuracy vs token cost.

**Pass bar:** You can explain what +200 tokens bought. Same `request_id` is idempotent.
