# Week 04 — Naive RAG

Parse → chunk → embed → retrieve → generate, with citations.

```powershell
docker compose --profile rag up -d
```

**Pass bar:** Unknown questions say "I don't know" and still list retrieved chunks.

Docs: https://docs.llamaindex.ai · https://github.com/pgvector/pgvector
